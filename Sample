$sched = "airflow-scheduler-7cf844b744-564tw"
$web   = "airflow-web-698b474997-2k4kh"

kubectl exec -n airflow $sched -- bash -c "cd /opt/airflow && tar -cf - logs" | kubectl exec -i -n airflow $web -- bash -c "mkdir -p /opt/airflow && tar -xf - -C /opt/airflow"
