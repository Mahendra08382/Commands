kubectl get deploy airflow-scheduler -n airflow -o yaml

apiVersion: v1
kind: Service
metadata:
  name: airflow-scheduler
  namespace: airflow
spec:
  ports:
  - port: 8793
    targetPort: 8793
    name: http-log
  selector:
    <match your scheduler pod labels>
