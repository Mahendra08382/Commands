kubectl -n airflow describe pod <name-of-imagepullbackoff-pod>
# look for events like ErrImagePull / ImagePullBackOff and the reason (401, 404, etc)
kubectl -n airflow logs <that-pod> -c <container-name>   # sometimes gives extra error msgs
