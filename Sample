kubectl -n airflow get deploy airflow-web -o "jsonpath={.spec.template.spec.containers[0].image}" -n airflow

kubectl -n airflow get deploy airflow-scheduler -o "jsonpath={.spec.template.spec.containers[0].image}" -n airflow
