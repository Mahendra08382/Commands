kubectl exec -n airflow pod/<scheduler-pod> -- ss -ltnp | grep 8793

apiVersion: v1
kind: Service
metadata:
  name: airflow-scheduler
  namespace: airflow
spec:
  ports:
    - port: 8793
      targetPort: 8793
  selector:
    component: scheduler   # <-- must match your pod label
