minikube ssh -- nslookup registry.gitlab.com  # should resolve quickly
kubectl rollout restart deploy airflow-web -n airflow
kubectl rollout restart deploy airflow-scheduler -n airflow
kubectl -n airflow logs deploy/airflow-scheduler --tail=200
